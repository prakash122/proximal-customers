describe('Process paths of input data', function () {
  test('When no paths are passed')
  test('When customer path is not existing throw error')
  test('when circle definition path is not existing throw error')
})