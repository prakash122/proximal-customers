{
  "name": "proximal-customers",
  "version": "0.1.0",
  "description": "Find all the customers who are in X km radius and invite them for food",
  "main": "index.js",
  "scripts": {
    "start": "node src/index.js",
    "lint": "eslint src/**",
    "test": "jest tests --verbose",
    "clean": "rm -fr output/*",
    "coverage": "jest --collectCoverageFrom=src/**.js --coverage tests"
  },
  "author": "Prakash Kumar <mail@prakash.io>",
  "license": "MIT",
  "devDependencies": {
    "eslint-plugin-jest": "^22.0.0",
    "jest": "^23.6.0",
    "rimraf": "^2.6.2"
  },
  "dependencies": {
    "mkdirp": "^0.5.1"
  }
}